# دليل الإعداد السريع

## خطوات التشغيل الأولى

### 1. تثبيت المكتبات
```bash
npm install
```

### 2. نقل مجلد الصور (مهم جداً!)
يجب نقل مجلد `image` إلى مجلد `public`:

**Windows (PowerShell):**
```powershell
Copy-Item -Path "image" -Destination "public\image" -Recurse
```

**Windows (CMD):**
```cmd
xcopy /E /I image public\image
```

**Mac/Linux:**
```bash
cp -r image public/image
```

### 3. تشغيل المشروع
```bash
npm run dev
```

سيتم فتح المشروع على `http://localhost:5173`

## التحقق من التثبيت

بعد نقل مجلد الصور، تأكد من وجود:
- ✅ `public/image/logo-removebg-preview.png`
- ✅ `public/image/hero (1).jpeg`
- ✅ `public/image/Aluminum kitchens/`
- ✅ `public/image/Wooden kitchens/`

إذا كانت الصور لا تظهر، تحقق من:
1. أن مجلد `image` موجود في `public/`
2. أن مسارات الصور في ملفات JSON صحيحة
3. أن أسماء الملفات تطابق تماماً (حساسة لحالة الأحرف)

## تخصيص المحتوى

### تعديل الخدمات
عدّل `src/data/services.json`

### تعديل المشاريع
عدّل `src/data/projects.json`

### تعديل المميزات
عدّل `src/data/features.json`

---

بعد الانتهاء من الإعداد، المشروع جاهز للاستخدام! 🚀

